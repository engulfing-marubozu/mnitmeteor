import { makeStyles } from "@mui/styles";
export const AdminPortelStyle = makeStyles({
  overlay: {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    minHeight:"100vh",
    backgroundColor: "#212121",
    zIndex: 1200,
  },
});
